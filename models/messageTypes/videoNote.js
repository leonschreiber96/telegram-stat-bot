import PhotoSize from '../entities/photosize';

let VideoNote = {
    file_id:  String,
    length:  Number,
    duration:  Number,
    file_size: Number,
    thumb: PhotoSize
};

export default VideoNote;