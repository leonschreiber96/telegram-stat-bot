import PhotoSize from '../entities/photosize';

let Audio = {
    file_id:  String,
    duration:  Number,
    performer: String,
    title: String,
    mime_type: String,
    file_size: Number,
    thumb: PhotoSize
};

export default Audio;